#include<stdio.h>
//author : Arsal Fadilah, V.01

int main(){
    int x, y;
    scanf("%d %d", &x, &y);
        for(x=x;x<=y;x++){
            printf("%d\n", x);
        }

    return 0;
}
